# leetcodes

```
pip install -r requirements.txt
```

Each problem is one file in `problems/`. Copy `template.py` to start.

```
pytest                         # run all
pytest problems/two_sum.py     # run one
pytest -x                      # stop on first failure
```
